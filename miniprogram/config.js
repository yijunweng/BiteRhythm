// miniprogram/config.js
// ============================================================
// 项目全局配置
// ============================================================

module.exports = {
  /**
   * 微信云开发环境 ID
   * 在微信开发者工具 → 云开发 → 环境 ID 中查找
   * 格式示例：your-env-abc123
   *
   * ⚠️ 必填，否则小程序无法连接云数据库
   */
  ENV_ID: 'cloud1-d1gowjtas225a3767'
};

/**
 * 关于超级管理员权限：
 * 无需在此配置 SUPER_ADMIN_OPENID。
 *
 * 首次打开小程序时，app.js 会自动调用 adminService 云函数，
 * 检测 system_config 数据库中是否已有超管记录：
 *   - 若没有：自动将第一个登录的用户设为超管
 *   - 若已有：与当前用户 openid 对比，匹配则授予超管权限
 *
 * 如需转移超管权限，请在微信云开发控制台的 system_config 集合中，
 * 修改 global_config 文档的 super_admin_openid 字段。
 */
