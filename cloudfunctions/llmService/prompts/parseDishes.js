module.exports = function({ text }) {
  return `你是一个食谱数据解析助手。请从下面这段用户输入的文本中，提取出菜名，识别它们属于哪个品类（分类仅限以下几个值之一：热菜, 凉菜, 汤品, 主食, 其它），并提取文案中针对该菜品的做法偏好（如「少油低盐」、「做法简单点」、「要炖烂」等。如果没有提及做法偏好，则设为空字符串）。
        
用户文本: "${text}"

输出要求:
1. 必须输出合法的 JSON 数组，不要有 Markdown 格式包装，不要任何额外话语。
格式规范:
[
  {"name": "菜品名称", "category": "品类", "practice": "做法偏好，无则为空字符串"}
]`;
};
